Typescript conversion
    Fix error handling in recursion and authenticate middleware
    Fix services return values (add return types to functions with multiple return statements; helps you think it out)
Expandable Stories/Templates Sidebar (how to handle state?)
Fix authContext (dispatch on logout/refresh token endpoints)
Fix error in searchbar where state doesn't update on delete.
Word-count (each root or branch storynode's wordcount is the sum of its children's wordcounts)
    For Root/Branch - call it something like 'Total Branch Wordcount:'
Branch-back-to-leaf
CSS App Layout:
    General improvements
    A subset of fixed styles
    Keep elements modular
    Light/Dark mode